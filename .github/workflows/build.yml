name: Docker Build Verification 

on:
  workflow_dispatch:
  push:
    branches:
      - master

env:
  DOTNET_VERSION: "9.x"
  APPHOST_PROJECT_PATH:  "src/aspire/CodeReview.AppHost/CodeReview.AppHost.csproj"
  
jobs:
  build_verification:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Clear NuGet Cache
        run: dotnet nuget locals all --clear

      - name: Install Aspire CLI
        run: dotnet tool install -g Aspire.ProjectTemplates --version 9.5.2-preview.1.25522.3

      - name: Restore dependencies
        run: dotnet restore

      - name: Aspire Publish and Generate Docker Compose
        run: dotnet publish ${{ env.APPHOST_PROJECT_PATH }} -c Release -o ./publish-output
        
      - name: Build Docker Images
        run: docker compose -f ./publish-output/docker-compose.yml build
        
    